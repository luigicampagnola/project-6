import { Particle } from "../Particle";
import { IPathValue } from "./IPathValue";
export interface IMovePathGenerator {
    init: () => void;
    update: () => void;
    generate: (particle: Particle) => IPathValue;
}
